# Pencil 资产总览

本目录改为“设计稿为主、文档最小化”。

## 保留内容
- `design-sources/`：全部设计资产（`.pen`），按页面/组件分层管理。
- `设计与研发协作规范.md`：唯一治理文档（职责、流程、命名、验收）。
- `Pencil-Skills.md`：Pencil 技能群（执行清单）。

## 约束
- 无 `.pen` 设计源，不进入开发。
- 设计组织按设计原则（布局/任务域/商城域/组件域）。
- 代码映射独立维护在 `design-sources/code-map.md`。
- 非必要不新增 Markdown；优先更新已有设计稿。
