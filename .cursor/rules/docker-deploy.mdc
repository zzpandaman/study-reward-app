---
description: Docker 部署配置规范
alwaysApply: true
---

# Docker 部署配置

## 部署位置

部署脚本位于 `/Users/cdmac14013/docker/star-deploy/`

## 前端部署（预留）

前端项目部署配置在 `${DEPLOY_DIR}/frontend/` 目录

### nginx 代理配置

编辑 `nginx/conf.d/frontend.conf`：

```nginx
server {
    listen 80;
    server_name app.example.com;

    location / {
        # 方式一：代理到前端开发服务器
        proxy_pass http://star-reward-app:3000;
        
        # 方式二：静态文件服务
        # root /usr/share/nginx/html/star-reward-app;
        # try_files $uri $uri/ /index.html;
    }
}
```

### 构建命令

```bash
npm run build
```

构建产物位于 `dist/` 目录

## API 地址配置

后端 API 通过 nginx 统一代理：

- SSO 服务：`/api/sso/*`
- Reward 服务：`/api/reward/*`

生产环境 API 配置：
```typescript
// src/api/config.ts
export const API_BASE_URL = '/api';
```
