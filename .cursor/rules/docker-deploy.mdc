---
description: Docker 部署配置规范
alwaysApply: true
---

# Docker 部署配置

## 部署方式

- **本地开发**: 使用部署脚本手动构建部署
- **生产环境**: GitHub Actions 自动构建上传

## 本地开发命令

### 前端服务管理

```bash
cd /Users/cdmac14013/docker/star-deploy/frontend

./deploy-frontend.sh deploy  # 构建并部署
./deploy-frontend.sh sync    # 仅同步（已构建）
./deploy-frontend.sh clean   # 清理
```

### 后端重启（修改后端代码后）

```bash
cd /Users/cdmac14013/docker/star-deploy/backend

./deploy-backend.sh deploy local   # 构建部署
./deploy-backend.sh restart local  # 仅重启
./deploy-backend.sh logs star-reward
./deploy-backend.sh status
```

### 一键全部部署

```bash
cd /Users/cdmac14013/docker/star-deploy
./deploy-all.sh local
```

## 健康检查

```bash
curl http://localhost/api/sso/health
curl http://localhost/api/reward/health
curl http://localhost/health
curl -I http://localhost/star/
```

## API 代理

Vite 开发服务器代理配置（`vite.config.ts`）：

```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost',  // nginx
      changeOrigin: true
    }
  }
}
```

## 构建命令

```bash
npm run build
```

构建产物位于 `dist/` 目录

---

## GitHub Actions 自动部署

### 触发条件

- push 到 main 分支
- 手动触发 (workflow_dispatch)

### 工作流程

```
push main → npm ci → npm run build → SCP 上传 dist → 重载 Nginx
```

### GitHub Secrets

```
SERVER_HOST     = <服务器IP>
SERVER_USER     = <SSH用户>
SERVER_SSH_KEY  = <SSH私钥>
```

### Workflow 文件

`.github/workflows/deploy.yml`

---

## 服务器运维

### 手动部署

```bash
# 本地构建后上传
npm run build
scp -r dist/* user@server:/opt/star-deploy/nginx/html/star/
ssh user@server "docker exec star-nginx nginx -s reload"
```

### Portainer

访问 `http://服务器IP/portainer/` 管理容器（通过 nginx 代理）

### 验证

```bash
curl -I http://服务器IP/star/
```
