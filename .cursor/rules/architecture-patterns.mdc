---
description: "架构规范与代码组织规则"
globs:
  - "src/**/*.ts"
  - "src/**/*.tsx"
alwaysApply: true
---

# 架构规范

本项目采用前后端分离，前端仅保留 API 层与视图层。

## 架构层次

### 1. 接口能力层 (API Layer)

**位置：** `src/api/`

**职责：**

- RESTful API 抽象
- 请求/响应类型定义

**规则：**

- ✅ API 类必须通过 `ApiClientFactory.getClient()` 获取 HttpClient
- ✅ 所有 API 方法为 `async`，返回 `Promise<ApiResponse<T>>`
- ✅ 请求/响应类型在 `api/types.ts` 中定义

**示例：**

```typescript
// ✅ 正确：API 类通过 HttpClient 调用后端
import ApiClientFactory from './client';

export class TaskTemplateAPI {
  static async getTaskTemplates(): Promise<ApiResponse<GetTaskTemplatesResponse>> {
    const client = ApiClientFactory.getClient();
    return client.post<GetTaskTemplatesResponse>('/api/task-templates/query', { page: 1, pageSize: 1000 });
  }
}
```

### 2. 视图渲染层 (View Layer)

**位置：** `src/components/`

**职责：**

- 用户界面渲染
- 用户交互处理

**规则：**

- ✅ 视图层通过 API 层获取数据
- ✅ UI 偏好（主题、customStyle）可暂存 localStorage

**示例：**

```typescript
// ✅ 正确：调用 API 层
import { TaskTemplateAPI } from '../api';
const response = await TaskTemplateAPI.getTaskTemplates();
```

## 环境配置

### 环境变量

```bash
# .env.development / .env.production
VITE_API_BASE_URL=/api/reward  # 默认，可省略
VITE_API_TIMEOUT=30000
```

### HttpClient

- 固定使用 HttpClient 调用 star-reward 后端
- baseURL 默认 `/api/reward`，可被 `VITE_API_BASE_URL` 覆盖
- 需 JWT Token 认证（SSO 登录后设置）

## 文件组织

```
src/
├── api/            # 接口层：API 类、HttpClient
├── components/     # 视图层：React 组件
└── utils/          # 工具函数：主题、样式等
```

## HttpClient 响应格式

后端返回 `{ code, message, data }`，HttpClient 自动转换为 `ApiResponse`：

```typescript
// 后端返回
{ code: 200, message: "操作成功", data: {...} }

// 自动转换为前端 ApiResponse
{ success: true, data: {...}, message: "操作成功" }
```

业务代码统一使用 `res.success` 判断：

```typescript
const res = await TaskTemplateAPI.getTaskTemplates();
if (res.success && res.data) {
  // 处理数据
}
```
