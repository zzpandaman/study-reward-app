---
description: API 客户端使用规范
globs: src/api/**/*.ts
alwaysApply: false
---

# API 客户端规范

## 响应格式

后端返回 `{code, message, data}`，HttpClient 自动转换为 `ApiResponse`：

```typescript
interface ApiResponse<T> {
  success: boolean;  // code === 200
  data?: T;
  message?: string;
  error?: string;    // code !== 200 时为 message
}
```

## 使用方式

```typescript
// ✅ 统一使用 success 判断
const res = await TaskTemplateAPI.getTaskTemplates();
if (res.success && res.data) {
  const items = res.data.data;  // 分页数据在 data.data
}

// ❌ 不要直接检查 code
if (res.code === 200) { ... }
```

## Token 管理

```typescript
import { setToken, clearToken } from './api';

// 登录后设置
setToken(response.data.token);

// 退出时清除
clearToken();
```

## 环境变量

```bash
# .env.local
VITE_API_BASE_URL=/api/reward  # 开发代理
VITE_API_TIMEOUT=30000
```
