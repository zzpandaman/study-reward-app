---
description: 前端与 star 微服务体系联动规范
alwaysApply: true
---

# 微服务体系联动（前端）

## 项目关系

```
study-reward-app (前端)
       │
       ├─→ star-sso (认证：登录、Token)
       └─→ star-reward (业务：任务、商品、积分)
```

## 环境配置

```bash
# .env.local 或 .env.production
VITE_API_BASE_URL=/api/reward   # 连接 star-reward
VITE_API_TIMEOUT=30000
```

## SSO 集成

### Token 管理

- 登录成功后 `setToken(response.data.token)` 存 localStorage
- 请求头 `Authorization: Bearer <token>`
- 401 时 `clearToken()` 并跳转登录

### 接口

- `POST /api/sso/login` - 登录
- `GET /api/sso/user/info` - 获取当前用户（需 Token）
- `POST /api/sso/logout` - 登出

## star-reward 集成

### API 路径

- baseURL 为 `/api/reward` 时，路径如 `/task-templates` → 完整 URL `/api/reward/task-templates`
- 兑换接口：`POST /api/reward/purchase-record/exchange`（非 `/user/exchange`）
- 积分记录：`POST /api/reward/point-records/query`

### 认证

- 所有 star-reward 请求需携带 JWT Token
- 未登录时 401，前端清除 Token 并展示登录
