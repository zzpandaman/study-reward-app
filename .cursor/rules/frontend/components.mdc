---
description: React 组件结构、数据获取与样式约定
globs:
  - "src/components/**/*.tsx"
alwaysApply: false
---

# 前端组件（src/components）

## 结构

- 组件放在 `src/components/`，文件 PascalCase（如 `TaskManager.tsx`）。
- 样式同名 CSS（如 `TaskManager.css`）；类名可用 kebab-case（`.task-manager`）。

## 数据

- 通过 API 层拉取数据（如 `TaskTemplateAPI`、`UserAPI`）；`res.success` 与数据结构见 `api/http-client.mdc`。
- 禁止组件绕过 API 访问未约定的内部数据层。

## 样式与主题

- 独立 CSS；可用 CSS 变量做主题（`--primary-color` 等）。
- 支持多主题与 CustomStyle（若产品已接入）。

## 状态与交互

- 本地状态：`useState`；副作用：`useEffect`，注意清理定时器等。
- 异步：`async/await`，按 `res.success` 处理结果。

## 业务约定（示例）

- 任务模板 / 商品：自定义可编辑；预设项不可删改（以产品为准）。
